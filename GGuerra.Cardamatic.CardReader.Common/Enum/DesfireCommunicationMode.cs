
namespace GGuerra.Cardamatic.CardReader.Common.Enum
{
    public enum DesfireCommunicationMode
    {
        /* < Plain mode of communication. */
        Plain = 0x00,
        /* < MAC mode of communication. */
        Mac = 0x01,
        /* < Enciphered mode of communication. */
        Enciphered = 0x03
    }
}
